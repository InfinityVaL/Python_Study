Problem 1. Working with files on the command line:
sort anna_words.txt | uniq -c > words_freq.txt
wc -l < words_freq.txt >> words_freq.txt


Problem 2. Working with files in Python 1:
string = []
with open('words_freq.txt') as file:
  text = file.readlines()
for line in text:
  line = line.strip(' \n')
  line = line.split(' ')
  line = tuple(line)
  string.append(line)
string = sorted(string, key = lambda s: s[0], reverse = True)
string[:10]


Problem 3. Working with files in Python 2:
max = 0
string = []
with open('words_freq.txt') as file:
  text = file.readlines()

for line in text:               # Редактирование потока, чтобы оставить только начальные символы.
  line = line.strip(' \n')
  line = line.split(' ')
  string.append(line[1][0])
  
string = sorted(string)         
uniq = set(string)              # Создание множества уникальных символов.
uniq = list(uniq)

for i in range(len(uniq)):      # Создаю внешний цикл, в котором перебираются уникальные начальные символы.
  count = 0

  for j in range(len(string)):  # Создаю внутренний цикл, в котором перебираются все начальные символы из основного потока и сравниваются со значениями во внешнем цикле.
    if uniq[i] == string[j]:
      count += 1

  if count > max:               # Поиск самого частого символа (буквы) и подсчёт его количества.
    max = count
    letter = []

  if count == max:              # Условие на случай, если количество искомых символов больше одного.
    letter.append(uniq[i])

print({max: letter})


Problem 4. Importing modules:
import math 
def circ_sqrt(r):
    C = 2*math.pi*r
    return C**0.5
r = 0
r = float(input('r = '))
circ_sqrt(r)


Problem 5. Installing external modules via pip:
!pip install cowsay
!pip install wikipedia
import cowsay
import wikipedia
def random_article(n):
  rand = wikipedia.random(n)
  sum = wikipedia.summary(rand, 1)
  result = cowsay.cow(sum)
  return result
random_article(1)


Problem 6. Bonus:
with open('anna_words.txt') as file:
  text = file.readlines()
text = sorted(text, key = len)
result = []
count = 0
x = 1

for word in text:
  word = word.strip('\n')
  length = 0
  for letter in word:
    length += 1
  if x == length:
    count += 1
  else:
    line = (length-1, count)
    result.append(line)
    x = length
    count = 1
print(result)

import pandas
table = pandas.DataFrame(result, columns = ['Length', 'Quantity'])
table.to_excel('Задание_6._Бонусное.xlsx')
